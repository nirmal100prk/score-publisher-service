# Default values for chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: score-publisher-service
  tag: "1.0"
  pullPolicy: IfNotPresent
service:
  type: NodePort
  port: 8080


